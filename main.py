"""Desktop entry point.

`flet build` packages the app starting from this file (its default entry).
It simply launches the GUI defined in the package.
"""
from email_export_import.gui.app import main

main()
